"use client"
import ResetForm from "@/app/auth/(components)/Reset";

export default function Home() {

	return (
		<main className="flex w-full justify-center items-center h-screen relative">
			<ResetForm />
		</main>
	);
}